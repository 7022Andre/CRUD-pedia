<!DOCTYPE html>
<html>
  <head>
    <title>CRUD-pedia - The Resource for Waste, Garbage and Trash</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  	<div class="main">
  		<ul class="nav nav-tabs">
  			<li><%= link_to "Home", root_path, class: "nav-tab" %></li>
        <li><%= link_to "Trash Stories", wikis_path, class: "nav-tab" %></li>
        <% if user_signed_in? %>
          <li class="pull-right"><%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: "nav-tab" %></li>
          <li class="pull-right"><%= link_to "Profile - #{current_user.email}", user_path(current_user), class: "nav-tab" %></li>
        <% else %>
          <li class="pull-right"><%= link_to "Log In", new_user_session_path, class: "nav-tab" %></li>
          <li class="pull-right"><%= link_to "Sign Up", new_user_registration_path, class: "nav-tab" %></li>
        <% end %>
  		</ul>

      <% if flash[:notice] %>
        <div class="alert alert-success">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= notice %>
        </div>
      <% elsif flash[:alert] %>
        <div class="alert alert-warning">
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <%= alert %><
        </div>
      <% end %>

    	<%= yield %>
    </div>
  </body>
</html>
